# 积分兑换与记录平台 — 实现文档

---

## 一、系统架构总览

```
┌──────────────────────────────────────────────────────┐
│                    Vercel 部署                        │
│  ┌────────────────────────────────────────────────┐  │
│  │         Next.js 15 (App Router)                 │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │  │
│  │  │ 前端页面  │  │ API 路由  │  │ Server       │  │  │
│  │  │ (RSC+    │  │ (Route   │  │ Actions      │  │  │
│  │  │  Client) │  │ Handlers)│  │              │  │  │
│  │  └──────────┘  └──────────┘  └──────────────┘  │  │
│  └────────────────────────────────────────────────┘  │
│                        │                             │
│                        ▼                             │
│  ┌────────────────────────────────────────────────┐  │
│  │         Vercel Postgres (Neon)                  │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │         Vercel Blob (礼物图片存储)               │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

**技术选型：**
| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Next.js 15 (App Router) | RSC + Server Actions 提升性能 |
| 样式 | Tailwind CSS v4 | 原子化样式，快速开发 |
| 动画 | Framer Motion | 页面过渡 + 庆祝动效 |
| 数据库 | Vercel Postgres (Neon) | Serverless 友好，含连接池 |
| 文件存储 | Vercel Blob | 礼物图片上传与托管 |
| 认证 | 自研 (phone + password) | 轻量，无需第三方 |
| 图标 | Lucide React | 活泼图标，体积小 |
| 动效 | canvas-confetti | 兑换成功撒花效果 |
| 音效 | use-sound + 自定义音效 | 点击、成功、升级等音效反馈 |
| 滚动动画 | react-intersection-observer | 元素进入视口时触发动画 |
| Excel | xlsx (SheetJS) | 批量导入导出 |

---

## 二、数据库设计

### ER 图

```
schools (学校) ─── 由管理员创建
┌──────────────┐
│ id           │
│ name         │
│ admin_id     │──┐
│ created_at   │  │
└──────────────┘  │
                  │
teachers (教师)    │  role: 'admin' | 'teacher'
┌──────────────┐  │
│ id           │  │
│ phone        │  │   classes (班级)
│ password_hash│  │  ┌──────────────┐
│ name         │  │  │ id           │
│ school_id    │◄─┘  │ name         │
│ role         │     │ teacher_id   │◄──┐
│ avatar_emoji │     │ created_at   │   │
│ created_at   │     └──────────────┘   │
└──────┬───────┘                        │
       │ 教师操作积分                    │
       ▼                                │
students (学生)                          │
┌──────────────┐                        │
│ id           │                        │
│ name         │                        │
│ class_id     │◄───────────────────────┘
│ points       │
│ avatar_emoji │
│ created_at   │
└──────┬───────┘
       │
       ├──────────────────────────────────┐
       │                                  │
       ▼                                  ▼
point_records (积分记录)        exchange_records (兑换记录)
┌──────────────────┐           ┌──────────────────┐
│ id               │           │ id               │
│ student_id       │           │ student_id       │
│ teacher_id       │           │ gift_id          │
│ points_change    │           │ points_spent     │
│ reason           │           │ exchanged_at     │
│ type (add/deduct)│           └──────────────────┘
│ created_at       │
└──────────────────┘

gifts (礼物)
┌──────────────┐
│ id           │
│ school_id    │  ← 礼物归属于学校
│ name         │
│ description  │
│ image_url    │
│ points_price │
│ stock        │
│ emoji        │
│ is_active    │
│ created_at   │
└──────────────┘
```

**角色说明：**
| 角色 | 权限 |
|------|------|
| **管理员 (admin)** | 创建学校、管理教师账号、管理全校数据、主题设置 |
| **教师 (teacher)** | 管理自己的班级/学生、积分操作、兑换操作、礼物管理 |

### SQL 建表语句

```sql
-- 学校表 (由系统管理员创建)
CREATE TABLE schools (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  admin_id UUID, -- 学校管理员 (在 teachers 表中)
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 教师表 (管理员手动添加，不允许自行注册)
CREATE TABLE teachers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone VARCHAR(11) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(50) NOT NULL,
  school_id UUID REFERENCES schools(id) ON DELETE CASCADE,
  role VARCHAR(20) DEFAULT 'teacher' CHECK (role IN ('admin', 'teacher')),
  avatar_emoji VARCHAR(10) DEFAULT '🧑‍🏫',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 班级表
CREATE TABLE classes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  teacher_id UUID NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 学生表
CREATE TABLE students (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(50) NOT NULL,
  class_id UUID NOT NULL REFERENCES classes(id) ON DELETE CASCADE,
  points INT DEFAULT 0 CHECK (points >= 0),
  avatar_emoji VARCHAR(10) DEFAULT '🌟',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 积分变动原因预设表 (学校级别共享)
CREATE TABLE reason_presets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  school_id UUID NOT NULL REFERENCES schools(id) ON DELETE CASCADE,
  teacher_id UUID REFERENCES teachers(id) ON DELETE SET NULL, -- NULL = 学校默认预设
  label VARCHAR(50) NOT NULL,
  type VARCHAR(10) NOT NULL CHECK (type IN ('add', 'deduct')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 积分变动记录表
CREATE TABLE point_records (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID NOT NULL REFERENCES students(id) ON DELETE CASCADE,
  teacher_id UUID NOT NULL REFERENCES teachers(id),
  points_change INT NOT NULL,
  reason VARCHAR(200) NOT NULL,
  type VARCHAR(10) NOT NULL CHECK (type IN ('add', 'deduct')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 礼物表 (归属于学校)
CREATE TABLE gifts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  school_id UUID REFERENCES schools(id) ON DELETE CASCADE,
  name VARCHAR(100) NOT NULL,
  description VARCHAR(500),
  image_url VARCHAR(500) NOT NULL,
  points_price INT NOT NULL CHECK (points_price > 0),
  stock INT DEFAULT -1, -- -1 表示无限库存
  emoji VARCHAR(10) DEFAULT '🎁',
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 兑换记录表
CREATE TABLE exchange_records (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID NOT NULL REFERENCES students(id),
  gift_id UUID NOT NULL REFERENCES gifts(id),
  points_spent INT NOT NULL,
  exchanged_at TIMESTAMPTZ DEFAULT NOW()
);

-- 教师设置表 (主题/音效/宠物偏好)
CREATE TABLE teacher_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  teacher_id UUID UNIQUE NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  theme VARCHAR(20) DEFAULT 'warm',           -- warm / candy / forest / ocean / sunset / dark
  sound_enabled BOOLEAN DEFAULT TRUE,
  sound_volume FLOAT DEFAULT 0.5 CHECK (sound_volume >= 0 AND sound_volume <= 1),
  pet_type VARCHAR(20) DEFAULT 'cat',         -- cat / dog / rabbit / hamster
  pet_name VARCHAR(20) DEFAULT '小可爱',
  animations_reduced BOOLEAN DEFAULT FALSE,   -- 减少动效模式
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 搜索性能索引

```sql
-- 学校搜索
CREATE INDEX idx_schools_name ON schools USING btree (name text_pattern_ops);

-- 教师搜索 (按学校 + 手机号)
CREATE INDEX idx_teachers_school_id ON teachers(school_id);
CREATE INDEX idx_teachers_phone ON teachers(phone);

-- 学生搜索
CREATE INDEX idx_students_name ON students USING btree (name text_pattern_ops);
CREATE INDEX idx_students_class_id ON students(class_id);

-- 班级搜索
CREATE INDEX idx_classes_name ON classes USING btree (name text_pattern_ops);
CREATE INDEX idx_classes_teacher_id ON classes(teacher_id);

-- 礼物搜索
CREATE INDEX idx_gifts_school_id ON gifts(school_id);
CREATE INDEX idx_gifts_name ON gifts USING btree (name text_pattern_ops);
CREATE INDEX idx_gifts_active ON gifts(is_active);

-- 记录查询
CREATE INDEX idx_point_records_student_id ON point_records(student_id);
CREATE INDEX idx_point_records_created_at ON point_records(created_at DESC);
CREATE INDEX idx_exchange_records_student_id ON exchange_records(student_id);
CREATE INDEX idx_exchange_records_exchanged_at ON exchange_records(exchanged_at DESC);
```

### 初始预设数据 (原因预设)

```sql
-- 加分原因预设
INSERT INTO reason_presets (teacher_id, label, type) VALUES
  (default_teacher_id, '积极回答问题', 'add'),
  (default_teacher_id, '作业完成优秀', 'add'),
  (default_teacher_id, '帮助同学', 'add'),
  (default_teacher_id, '课堂纪律好', 'add'),
  (default_teacher_id, '考试成绩进步', 'add'),
  (default_teacher_id, '主动打扫卫生', 'add');

-- 扣分原因预设
INSERT INTO reason_presets (teacher_id, label, type) VALUES
  (default_teacher_id, '未完成作业', 'deduct'),
  (default_teacher_id, '上课讲话', 'deduct'),
  (default_teacher_id, '迟到', 'deduct'),
  (default_teacher_id, '不遵守纪律', 'deduct');
```

---

## 三、目录结构

```
jifen/
├── app/
│   ├── layout.tsx              # 根布局 (主题提供者 + 像素宠物)
│   ├── page.tsx                # 首页 (重定向到 dashboard)
│   ├── login/
│   │   └── page.tsx            # 登录页 (密码可见切换)
│   ├── dashboard/
│   │   └── page.tsx            # 仪表盘 (教师/管理员共用)
│   ├── admin/                  # [新增] 管理员专区
│   │   ├── page.tsx            # 管理员首页
│   │   ├── schools/
│   │   │   ├── page.tsx        # 学校管理 (CRUD + 列表)
│   │   │   └── actions.ts      # 学校 Server Actions
│   │   ├── teachers/
│   │   │   ├── page.tsx        # 教师账号管理 (添加/禁用/重置密码)
│   │   │   └── actions.ts      # 教师管理 Server Actions
│   │   └── layout.tsx          # 管理员布局 (含权限校验)
│   ├── classes/
│   │   ├── page.tsx            # 班级列表
│   │   ├── [id]/
│   │   │   └── page.tsx        # 班级详情(学生列表)
│   │   └── actions.ts          # Server Actions (班级CRUD)
│   ├── students/
│   │   ├── [id]/
│   │   │   └── page.tsx        # 学生详情(积分记录)
│   │   └── actions.ts          # Server Actions (学生CRUD + 积分)
│   ├── exchange/
│   │   ├── page.tsx            # 礼物兑换台
│   │   └── actions.ts          # Server Actions (兑换逻辑)
│   ├── gifts/
│   │   ├── page.tsx            # 礼物管理(教师)
│   │   └── actions.ts          # Server Actions (礼物CRUD)
│   ├── records/
│   │   └── page.tsx            # 积分与兑换汇总记录
│   ├── settings/               # [新增] 主题与音效设置
│   │   ├── page.tsx            # 设置页 (主题/音效/宠物)
│   │   └── actions.ts          # 设置 Server Actions
│   └── api/
│       ├── auth/
│       │   └── login/route.ts  # POST 登录接口
│       ├── upload/
│       │   └── route.ts        # 图片上传接口
│       └── import/
│           └── route.ts        # Excel 批量导入接口
├── components/
│   ├── ui/                     # 基础 UI 组件
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Modal.tsx
│   │   ├── Input.tsx
│   │   ├── Select.tsx
│   │   ├── Badge.tsx
│   │   ├── Toast.tsx
│   │   ├── SearchInput.tsx     # 通用搜索框 (debounce + 彩虹动画)
│   │   ├── Pagination.tsx      # 通用分页器 (翻转动画)
│   │   ├── Skeleton.tsx        # 骨架屏加载占位
│   │   ├── EmptyState.tsx      # 空状态趣味插画
│   │   └── RainbowDivider.tsx  # 彩虹分割线
│   ├── layout/
│   │   ├── Sidebar.tsx         # 活泼侧边导航
│   │   ├── Header.tsx          # 顶部栏 (含主题切换按钮)
│   │   ├── ThemeToggle.tsx     # [新增] 主题切换器
│   │   ├── PixelPet.tsx        # 像素宠物动画组件
│   │   ├── FloatingBubbles.tsx # 背景漂浮气泡
│   │   └── RainbowTrail.tsx    # 鼠标彩虹拖尾
│   ├── classes/
│   │   ├── ClassCard.tsx       # 班级卡片
│   │   ├── ClassForm.tsx       # 班级表单
│   │   ├── ClassList.tsx       # 班级列表 (含分页搜索)
│   │   └── ImportModal.tsx     # 批量导入弹窗
│   ├── students/
│   │   ├── StudentCard.tsx     # 学生卡片 (积分动画)
│   │   ├── StudentForm.tsx     # 学生表单
│   │   ├── StudentList.tsx     # 学生列表 (含分页搜索)
│   │   ├── PointAdjust.tsx     # 积分加减组件 (步进器动画)
│   │   ├── PointHistory.tsx    # 积分变动时间线
│   │   └── RankBadge.tsx       # 排名徽章 (火箭动画)
│   ├── exchange/
│   │   ├── GiftCard.tsx        # 礼物卡片
│   │   ├── GiftGrid.tsx        # 礼物网格 (含分页搜索)
│   │   ├── ExchangeModal.tsx   # 兑换确认弹窗
│   │   ├── Confetti.tsx        # 撒花组件
│   │   └── MilestoneEffect.tsx # 积分里程碑特效
│   ├── admin/                  # [新增] 管理员组件
│   │   ├── SchoolCard.tsx      # 学校卡片
│   │   ├── SchoolForm.tsx      # 学校表单
│   │   ├── TeacherCard.tsx     # 教师账号卡片
│   │   └── TeacherForm.tsx     # 教师添加/编辑表单
│   └── gifts/
│       ├── GiftForm.tsx        # 礼物表单
│       ├── GiftImageUpload.tsx # 图片上传
│       └── GiftManageList.tsx  # 礼物管理列表 (含分页搜索)
├── lib/
│   ├── db.ts                   # 数据库连接
│   ├── auth.ts                 # 认证工具 (session/jwt + 角色校验)
│   ├── blob.ts                 # Vercel Blob 工具
│   ├── utils.ts               # 通用工具
│   ├── holidays.ts            # 节日检测
│   └── themes.ts              # [新增] 主题定义与配置
├── hooks/
│   ├── useAuth.ts              # 认证 hook
│   ├── useToast.ts            # Toast 通知 hook
│   ├── useDebounce.ts         # 防抖 hook (搜索用)
│   ├── useSound.ts             # 音效 hook
│   ├── useTheme.ts            # [新增] 主题切换 hook
│   ├── useHolidayTheme.ts     # 节日主题 hook
│   └── useInView.ts           # 元素可见性 (滚动触发动画)
├── types/
│   └── index.ts               # TypeScript 类型定义
├── public/
│   ├── pets/                   # 像素宠物精灵图 (多状态多角色)
│   ├── sounds/                 # 8-bit 音效文件 (来源见 "音效资源" 章节)
│   │   ├── pop.mp3             # → Freesound #448086 (Breviceps/UIClick1)
│   │   ├── success.mp3         # → Freesound 搜索 "8-bit success collect"
│   │   ├── error.mp3           # → Zapsplat 8-Bit Blips 包
│   │   ├── swoosh.mp3          # → Freesound #60013 (qubodup/Whoosh)
│   │   ├── coin.mp3            # → Freesound 搜索 "8-bit coin collect CC0"
│   │   └── celebration.mp3     # → Zapsplat 8-Bit Blips 包
│   └── illustrations/          # 空状态/趣味插画
├── .env.local                  # 环境变量
├── tailwind.config.ts
├── next.config.ts
└── package.json
```

---

## 四、页面路由设计

| 路由 | 页面名称 | 权限 | 说明 |
|------|---------|------|------|
| `/login` | 登录页 | 公开 | 手机号 + 密码登录，**密码默认可见** |
| `/dashboard` | 仪表盘 | 教师/管理员 | 数据概览卡片 + 快捷入口 |
| `/classes` | 班级管理 | 教师/管理员 | 班级列表，CRUD + 批量导入 |
| `/classes/[id]` | 学生列表 | 教师/管理员 | 某班级下学生，积分加减 |
| `/students/[id]` | 学生详情 | 教师/管理员 | 积分变动记录时间线 |
| `/exchange` | 兑换台 | 教师/管理员 | 展示所有礼物，选择学生兑换 |
| `/gifts` | 礼物管理 | 教师/管理员 | CRUD + 图片上传 |
| `/records` | 记录总览 | 教师/管理员 | 积分 + 兑换汇总记录 |
| `/settings` | **[新增]** 设置 | 教师/管理员 | 主题切换 / 音效开关 / 宠物选择 |
| `/admin` | **[新增]** 管理员首页 | 管理员 | 学校与教师概览 |
| `/admin/schools` | **[新增]** 学校管理 | 管理员 | 学校 CRUD + 列表 |
| `/admin/teachers` | **[新增]** 教师管理 | 管理员 | 教师账号 CRUD + 禁用/重置密码 |

---

## 五、页面 UI 设计规范

### 5.1 色彩系统

```
主色调:
  暖橙 #FF8C42   — 主按钮、强调元素
  阳光黄 #FFD166 — 卡片背景、积分徽章
  薄荷绿 #06D6A0 — 加分、成功状态
  珊瑚粉 #EF476F — 扣分、警告状态
  天空蓝 #118AB2 — 链接、信息提示

背景:
  奶油白 #FFF9F0 — 页面主背景
  浅粉 #FFF0F0 — 卡片交替背景

文字:
  深棕 #4A3728 — 主文字
  中灰 #8B7E74 — 辅助文字
```

### 5.2 字体与圆角

```css
/* 活泼圆体作为标题 */
--font-display: 'Mochiy Pop One', 'ZCOOL KuaiLe', sans-serif;
/* 圆体作为正文 */
--font-body: 'Nunito', 'Noto Sans SC', sans-serif;

/* 圆角 */
--radius-sm: 10px;
--radius-md: 16px;
--radius-lg: 24px;
--radius-xl: 32px;
--radius-full: 9999px;
```

### 5.3 核心页面设计

#### 登录页 (`/login`)

```
┌──────────────────────────────────────────────┐
│                                              │
│     🐱 像素小猫 (左右漂浮动画)                │
│                                              │
│         ┌─────────────────────┐              │
│         │  🌟 积分乐园 🌟     │              │
│         │                     │              │
│         │  📱 手机号          │              │
│         │  ┌───────────────┐  │              │
│         │  │ 13812341234   │  │              │
│         │  └───────────────┘  │              │
│         │                     │              │
│         │  🔓 密码 (明文可见) │              │
│         │  ┌───────────────┐  │              │
│         │  │ 123456     👁️🗨️│  │  ← 密码默认明文可见
│         │  └───────────────┘  │     点击眼睛图标切换隐藏
│         │                     │
│         │  [ 🚀 冲进乐园 ]    │
│         │  (大圆角按钮)       │
│         │                     │
│         │  💡 没有账号？      │
│         │  请联系管理员添加   │  ← 明确说明不开放注册
│         └─────────────────────┘              │
│                                              │
│     🌈 彩虹装饰条 (底部)                      │
└──────────────────────────────────────────────┘
```

**密码可见切换组件实现：**

```tsx
// 密码输入框 — 默认明文可见，可切换隐藏
'use client';
import { useState } from 'react';
import { Eye, EyeOff } from 'lucide-react';

export function PasswordInput() {
  const [visible, setVisible] = useState(true);  // 默认可见！

  return (
    <div className="relative">
      <input
        type={visible ? 'text' : 'password'}   // text → 明文；password → 圆点
        placeholder="请输入密码"
        className="w-full pr-10 ..."
      />
      <button
        type="button"
        onClick={() => setVisible(!visible)}
        className="absolute right-3 top-1/2 -translate-y-1/2"
        aria-label={visible ? '隐藏密码' : '显示密码'}
      >
        {visible ? <EyeOff size={20} /> : <Eye size={20} />}
      </button>
    </div>
  );
}
```

#### 仪表盘 (`/dashboard`)

```
┌──────────────────────────────────────────────────────┐
│  👋 你好，李老师！           🌻⛅🌈  早上好!           │
│  (根据时间切换问候语 + 天气装饰)                      │
├──────────┬──────────┬──────────┬────────────────────┤
│  🎓      │  ⭐      │  🎁      │  📋                │
│ 5 个班级 │ 128 学生 │ 23 件礼物│ 今日 12 次兑换      │
│ (带计数  │ (带计数  │ (带计数  │ (带计数             │
│  弹跳)   │  弹跳)   │  弹跳)   │  弹跳)              │
├──────────┴──────────┴──────────┴────────────────────┤
│                                                      │
│  快捷入口:                                           │
│  [📚 管理班级] [🎁 兑换礼物] [📊 查看记录] [⚙️ 设置]  │
│     (悬停弹跳)                                        │
│                                                      │
│  ┌─────────────────────┐ ┌────────────────────────┐  │
│  │ 🏆 积分排行榜 Top 5  │ │ 📋 最近动态             │  │
│  │                     │ │                        │  │
│  │ 🥇 小明  250分      │ │ 🎁 小明 兑换 棒棒糖     │  │
│  │ 🥈 小红  180分 🚀↑  │ │   2分钟前              │  │
│  │ 🥉 小刚  150分      │ │ ➕ 小红 +5分 积极回答问题│  │
│  │ 4. 小丽  120分 🚀↑  │ │   10分钟前             │  │
│  │ 5. 小强  100分      │ │ ➖ 小刚 -3分 未完成作业  │  │
│  │                     │ │   30分钟前             │  │
│  └─────────────────────┘ └────────────────────────┘  │
│                                                      │
│  ☀️ 今日天气不错，适合给小朋友们发积分哦~              │
│                                                      │
│  🐱 像素小猫 — 在右下角打哈欠 (早上模式)              │
│  🫧 背景飘着淡色的气泡                                │
└──────────────────────────────────────────────────────┘
```

#### 礼物管理 (`/gifts`)

```
┌──────────────────────────────────────────────────────┐
│  🎁 礼物管理                                         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │🔍 搜索礼物...  │ │ 排序: [最新▼] │ │ [+ 添加礼物]  │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│  共 23 件礼物 (已上架 20 / 已下架 3)                   │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 🍭       │  │ 📓       │  │ 🧸       │          │
│  │ [图片]   │  │ [图片]   │  │ [图片]   │          │
│  │ 棒棒糖    │  │ 笔记本    │  │ 小熊玩偶  │          │
│  │ ⭐ 50分  │  │ ⭐ 100分 │  │ ⭐ 300分 │          │
│  │ 库存: ∞  │  │ 库存: 20  │  │ 库存: 5   │          │
│  │ 🟢 上架中 │  │ 🟢 上架中 │  │ 🟢 上架中 │          │
│  │ [✏️] [👁] │  │ [✏️] [👁] │  │ [✏️] [👁] │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                      │
│                     « ‹ 1 2 › »                      │
└──────────────────────────────────────────────────────┘
```

#### 礼物兑换台 (`/exchange`)

```
┌──────────────────────────────────────────────────────┐
│  🎪 积分兑换小铺                                     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │🔍 搜索礼物...  │ │🏷 价格: [全部▼]│ │👤 学生: [选择▼]│  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│  当前学生: 小明 🌟  余额: ⭐ 250 分                   │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 🍭       │  │ 📓       │  │ 🧸       │          │
│  │ [图片]   │  │ [图片]   │  │ [图片]   │          │
│  │          │  │          │  │          │          │
│  │ 棒棒糖    │  │ 笔记本    │  │ 小熊玩偶  │          │
│  │ ⭐ 50分  │  │ ⭐ 100分 │  │ ⭐ 300分 │          │
│  │          │  │          │  │          │          │
│  │ [兑换!]  │  │ [兑换!]  │  │ [积分不足]│          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 🎨       │  │ ✏️       │  │ 🎈       │          │
│  │ [图片]   │  │ [图片]   │  │ [图片]   │          │
│  │          │  │          │  │          │          │
│  │ 水彩笔    │  │ 卡通橡皮  │  │ 气球      │          │
│  │ ⭐ 80分  │  │ ⭐ 30分  │  │ ⭐ 60分  │          │
│  │ [兑换!]  │  │ [兑换!]  │  │ [兑换!]  │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                      │
│  ... 更多礼物 (共 23 件)                              │
│                    « ‹ 1 2 › »                       │
│                                                      │
│  🐱 像素宠物在角落兴奋地跳动                           │
└──────────────────────────────────────────────────────┘

点击兑换 →
┌──────────────────────┐
│  🧸 确认兑换         │
│                      │
│  [礼物图片]          │
│  小熊玩偶            │
│  消耗 ⭐ 300 分      │
│                      │
│  学生: 小明          │
│  当前: 250 分        │
│  ⚠️ 积分不足!        │
│                      │
│  [再想想] [去加分]   │
└──────────────────────┘
或者 (积分足够时):
┌──────────────────────┐
│  🧸 确认兑换         │
│  兑换后余额: 50 分   │
│  [再想想] [确认兑换] │
└──────────────────────┘
确认后 → 🎉 撒花 + 宠物跳起 + 音效 + Toast
```

#### 班级详情-学生列表 (`/classes/[id]`)

```
┌──────────────────────────────────────────────────────┐
│  ← 返回    📚 三年级一班           [📥 导入] [+ 添加] │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │🔍 搜索学生...  │ │ 排序: [积分▼] │ │🏆 积分排行榜  │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│  共 38 名学生                      « ‹ 1 2 3 4 › »   │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────┐  ┌──────────────────────┐  │
│  │ 🥇 小明   ⭐ 250 分   │  │ 🥈 小红   ⭐ 180 分   │  │
│  │ 排名: #1  ↑2         │  │ 排名: #2  ↓1         │  │
│  │                      │  │                      │  │
│  │ [＋加分] [−扣分]      │  │ [＋加分] [−扣分]      │  │
│  │ [📋 查看记录]         │  │ [📋 查看记录]         │  │
│  └──────────────────────┘  └──────────────────────┘  │
│                                                      │
│  ┌──────────────────────┐  ┌──────────────────────┐  │
│  │ 🥉 小刚   ⭐ 150 分   │  │ 🌟 小丽   ⭐ 120 分   │  │
│  │ 排名: #3  —          │  │ 排名: #4  ↑5 🚀      │  │
│  │                      │  │                      │  │
│  │ [＋加分] [−扣分]      │  │ [＋加分] [−扣分]      │  │
│  │ [📋 查看记录]         │  │ [📋 查看记录]         │  │
│  └──────────────────────┘  └──────────────────────┘  │
│                                                      │
│                     « ‹ 1 2 3 4 › »                  │
└──────────────────────────────────────────────────────┘

点击 ＋/− 弹出积分调整弹窗 →
┌──────────────────────────┐
│  ✨ 调整小明积分          │
│                          │
│  当前: ⭐ 250 分         │
│                          │
│  类型: [➕ 加分] [➖ 扣分]│
│         (选中弹跳放大)    │
│                          │
│  分数: [− 1 +] ← 步进器  │
│        (长按连续加减)     │
│                          │
│  原因:                   │
│  ┌──────────────────┐   │
│  │ 积极回答问题    ▼ │   │
│  └──────────────────┘   │
│                          │
│  或自定义:               │
│  [__________________]   │
│                          │
│       [✨ 确认调整]      │
│  (确认后：数字弹跳动画    │
│   + 宠物 👍 + 金币音效)  │
└──────────────────────────┘
```

#### 礼物管理 (`/gifts`)

```
┌────────────────────────────────────────────────────┐
│  🎁 礼物管理                         [+ 添加礼物]   │
├────────────────────────────────────────────────────┤
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 🍭       │  │ 📓       │  │ 🧸       │        │
│  │ [图片]   │  │ [图片]   │  │ [图片]   │        │
│  │ 棒棒糖    │  │ 笔记本    │  │ 小熊玩偶  │        │
│  │ ⭐ 50分  │  │ ⭐ 100分 │  │ ⭐ 300分 │        │
│  │ 库存: ∞  │  │ 库存: 20  │  │ 库存: 5   │        │
│  │ [编辑]   │  │ [编辑]   │  │ [编辑]   │        │
│  │ [下架]   │  │ [下架]   │  │ [下架]   │        │
│  └──────────┘  └──────────┘  └──────────┘        │
└────────────────────────────────────────────────────┘
```

#### **[新增]** 管理员 — 学校管理 (`/admin/schools`)

```
┌──────────────────────────────────────────────────────┐
│  🏫 学校管理                        [+ 创建学校]      │
│  ┌──────────────┐ ┌──────────────┐                   │
│  │🔍 搜索学校...  │ │ 排序: [最新▼] │                   │
│  └──────────────┘ └──────────────┘                   │
│  共 3 所学校                        « ‹ 1 › »        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ 🏫 阳光小学                                   │    │
│  │    管理员: 李老师 📱 138****1234               │    │
│  │    教师: 12 人 | 学生: 256 人 | 班级: 18 个    │    │
│  │    [👁 查看] [✏️ 编辑] [🗑️ 删除]               │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ 🏫 星光小学                                   │    │
│  │    管理员: 待指定 ⚠️                           │    │
│  │    教师: 3 人 | 学生: 45 人 | 班级: 3 个       │    │
│  │    [👁 查看] [✏️ 编辑] [🗑️ 删除]               │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│                     « ‹ 1 › »                        │
└──────────────────────────────────────────────────────┘
```

#### **[新增]** 管理员 — 教师账号管理 (`/admin/teachers`)

```
┌──────────────────────────────────────────────────────┐
│  👨‍🏫 教师账号管理                     [+ 添加教师]     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │🔍 搜索教师...  │ │🏫 学校: [全部▼]│ │ 状态: [全部▼] │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│  共 15 名教师 (13 启用 / 2 禁用)   « ‹ 1 2 › »       │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ 🧑‍🏫 李老师    📱 13812341234    🟢 启用       │    │
│  │    学校: 阳光小学   角色: 学校管理员            │    │
│  │    [✏️ 编辑] [🔑 重置密码] [🚫 禁用]           │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ 🧑‍🏫 王老师    📱 13956789012    🔴 已禁用     │    │
│  │    学校: 阳光小学   角色: 教师                 │    │
│  │    [✏️ 编辑] [🔑 重置密码] [✅ 启用]           │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│                     « ‹ 1 2 › »                      │
└──────────────────────────────────────────────────────┘
```

添加教师弹窗：
```
┌──────────────────────────┐
│  ✨ 添加新教师            │
│                          │
│  姓名:                   │
│  [__________________]   │
│                          │
│  手机号:                 │
│  [__________________]   │
│                          │
│  所属学校:               │
│  ┌──────────────────┐   │
│  │ 阳光小学       ▼ │   │
│  └──────────────────┘   │
│                          │
│  角色:                   │
│  ○ 学校管理员  ● 教师    │
│                          │
│  初始密码:               │
│  [123456]  🎲 随机生成   │
│                          │
│      [确认添加]          │
└──────────────────────────┘
```

#### **[新增]** 设置页面 (`/settings`)

```
┌──────────────────────────────────────────────────────┐
│  ⚙️ 个性化设置                                       │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 🎨 主题外观                                   │   │
│  │                                              │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │   │
│  │  │ 🧡   │ │ 🍬   │ │ 🌲   │ │ 🌊   │       │   │
│  │  │暖橙主题│ │糖果主题│ │森林主题│ │海洋主题│       │   │
│  │  │ (默认) │ │ 粉色  │ │ 绿色  │ │ 蓝色  │       │   │
│  │  └──────┘ └──────┘ └──────┘ └──────┘       │   │
│  │  ┌──────┐ ┌──────┐                          │   │
│  │  │ 🌅   │ │ 🌙   │                          │   │
│  │  │日落主题│ │暗夜主题│                          │   │
│  │  │ 紫色  │ │ 深色  │                          │   │
│  │  └──────┘ └──────┘                          │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 🔊 音效设置                                   │   │
│  │                                              │   │
│  │  音效开关: [🟢 开启] [⚪ 关闭]                 │   │
│  │  音量:     [━━━━━━━○────] 70%                 │   │
│  │  [▶️ 试听 pop] [▶️ 试听 coin] [▶️ 试听 success]│   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 🐾 像素宠物                                   │   │
│  │                                              │   │
│  │  宠物:  [🐱小猫] [🐶小狗] [🐰小兔] [🐹仓鼠]    │   │
│  │  昵称:  [小可爱____________]                  │   │
│  │  预览:  🐱 (宠物实时演示当前选中动画)           │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 🎬 动效设置                                   │   │
│  │                                              │   │
│  │  ☑️ 启用页面过渡动画                          │   │
│  │  ☑️ 启用卡片入场动画                          │   │
│  │  ☑️ 启用撒花/里程碑特效                       │   │
│  │  ☐ 减少动效 (无障碍模式)                      │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│                   [💾 保存设置]                       │
└──────────────────────────────────────────────────────┘
```

### 5.4 像素宠物系统

在页面右下角常驻一个像素宠物，作为整个应用的"灵魂角色"。

**宠物设计规格：**
- 尺寸：64×64px (桌面) / 48×48px (平板)
- 位置：固定在右下角，距边缘 20px，z-50
- 实现：CSS Sprite 精灵图动画 (8帧/状态) 或 Lottie JSON
- 可选宠物：🐱 小猫 (默认)、🐶 小狗、🐰 小兔、🐹 仓鼠 (教师在设置中选择)

**宠物动画状态机：**

```
                    ┌─────────────────────┐
                    │     😴 睡觉         │
                    │  (3分钟无操作)       │
                    └────────┬────────────┘
                             │ 鼠标移动/点击页面任意处
                             ▼
┌──────────┐  页面加载   ┌──────────┐  点击宠物  ┌──────────┐
│ ✨ 出场   │ ─────────► │ 😊 空闲   │ ─────────►│ 💬 说话   │
│ (从右下  │            │ (呼吸动画) │           │ 随机气泡   │
│  弹入)   │            └─────┬─────┘           └──────────┘
└──────────┘                 │
                             │ 兑换成功事件
                             ▼
                      ┌──────────┐
                      │ 🎉 庆祝   │
                      │ (跳3次+  │
                      │  撒星星) │
                      └─────┬────┘
                             │
                             │ 积分增加事件
                             ▼
                      ┌──────────┐
                      │ 👍 点赞   │
                      │ (竖大拇指)│
                      └──────────┘
                             │
                             │ 积分扣减事件
                             ▼
                      ┌──────────┐
                      │ 😢 安慰   │
                      │ (摇头+眼泪│
                      │  表情)    │
                      └──────────┘
                             │
                             │ 每30s随机
                             ▼
                      ┌──────────┐
                      │ 🏃 随机动作│
                      │ 走路/跳跃/│
                      │ 转圈/翻跟头│
                      └──────────┘
```

**宠物对话气泡：** 根据上下文随机展示：
- 空闲时：「今天小朋友们都好棒呀~ 🌟」「老师辛苦啦~ ☕」「记得给表现好的同学加分哦~」
- 兑换时：「哇！又兑换礼物啦！🎉」「这个礼物我也想要~ 🎁」
- 加分时：「太厉害了！👍」「继续加油！💪」
- 扣分时：「下次注意哦~ 💪」「不要灰心！🌈」
- 点击时：「嘻嘻，好痒~ 🤭」「老师点我干嘛呀~ 😆」

### 5.5 动画系统详细设计

#### 5.5.1 页面级动画

| 动画名 | 触发时机 | 实现方式 | 时长 |
|--------|---------|---------|------|
| **页面入场** | 路由切换 | Framer Motion `AnimatePresence`，从下方 20px 淡入 | 0.3s |
| **页面退场** | 路由切换 | 向上 10px 淡出 | 0.2s |
| **彩虹跟随** | 鼠标移动 | 光标后拖尾彩虹粒子 (5个彩色圆点依次跟随) | 持续 |
| **背景浮动** | 页面空闲 | 背景漂浮彩色气泡/星星 (随机大小/透明度/速度) | 无限循环 |

#### 5.5.2 组件级动画

**卡片动画：**
| 动画名 | 描述 | 实现 |
|--------|------|------|
| **入场交错** | 卡片列表依次从下方弹入 | Framer Motion `staggerChildren: 0.05` |
| **悬停浮起** | 卡片上浮 8px + 阴影放大 + 轻微旋转 (±1deg) | `whileHover` + spring 弹性曲线 |
| **点击波纹** | 卡片被点击时从点击处扩散圆形波纹 | CSS `::after` 伪元素 + scale 动画 |
| **积分变化闪烁** | 分数变动时卡片边框闪烁金色 3 次 | CSS `@keyframes shimmer` |

**按钮动画：**
| 动画名 | 描述 | 实现 |
|--------|------|------|
| **弹性按下** | 按下时缩小到 0.92，松开弹回 1.0 | `whileTap={{ scale: 0.92 }}` spring |
| **成功脉冲** | 兑换/加分成功后按钮绿色脉冲扩散 | `box-shadow` 动画扩散 |
| **摇晃拒绝** | 积分不足时按钮左右摇晃 | `animate={{ x: [0,-5,5,-5,5,0] }}` |
| **流光扫过** | 主按钮上高光从左到右扫过 | `background` gradient 位移动画 |

**Modal/弹窗动画：**
| 动画名 | 描述 |
|--------|------|
| **弹入** | 从下方弹入 + 缩放 (0.8→1.05→1.0) |
| **背景渐显** | 半透明背景从 0 到 0.5 透明度 |
| **聚焦抖动** | 弹窗内输入框获得焦点时微微抖动 |
| **确认弹跳** | 点击确认后按钮变为 ✓ 图标并弹跳 |

**积分数字动画：**
```
加分动画流程:
  1. 旧数字缩小淡出 (0.15s)
  2. "+N" 文字从下方弹入，金色大字 (0.3s)
  3. "+N" 停留 0.5s 后上浮淡出
  4. 新数字从上方弹入，放大约 1.2 倍后回弹到正常 (0.3s)
  5. 数字背景闪烁金色 → 恢复 (0.3s)

扣分动画流程:
  1. 旧数字微微抖动 (0.2s)
  2. "-N" 文字淡入，红色，数字缩小
  3. 新数字替换，短暂红色背景闪烁后恢复
```

#### 5.5.3 场景动效

**兑换成功 — 完整庆祝流程 (2s)：**
```
阶段1 (0-0.3s):  确认弹窗关闭，卡片缩小飞出
阶段2 (0.3-1s):  canvas-confetti 撒花 (彩色纸条 + 星星，从中心爆发)
阶段3 (0.5-1.5s): 宠物跳起 + ❤️ + "太棒了!" 气泡
阶段4 (1-1.5s):  页面中央闪现大 🎉 emoji，放大后淡出
阶段5 (1.5-2s):  音效 "叮叮叮~" (可选)
```

**积分排行榜升级 — 排名上升动画：**
```
如果学生积分变化导致排名上升:
  1. 排名数字闪烁 → 旧排名划掉 → 新排名弹入
  2. 一个像素小火箭 🚀 从卡片底部飞升
```

**首次登录欢迎动画：**
```
登录成功后:
  1. 像素宠物从右下角跑到屏幕中央
  2. 跳起来展开横幅 "欢迎回来，李老师！🌈"
  3. 横幅停留 1.5s 后卷起收回，宠物跑回角落
```

#### 5.5.4 趣味交互细节

**音效系统：** (使用 use-sound 库，所有音效为轻量 8-bit 风格)
| 音效 | 场景 | 描述 |
|------|------|------|
| `pop.mp3` | 点击按钮 | 清脆气泡爆裂声 |
| `success.mp3` | 兑换/加分成功 | 三连上升音阶 |
| `error.mp3` | 扣分/兑换失败 | 轻轻下沉音 |
| `swoosh.mp3` | Modal 弹出 | 嗖的一声 |
| `coin.mp3` | 积分变动 | 金币叮当声 |
| `celebration.mp3` | 撒花 | 欢快小号声 |

**微交互清单：**
- **侧边栏菜单项**: 悬停时图标抖动 + 背景色左右滑动
- **搜索框**: 聚焦时边框彩虹渐变流动；输入文字时每个字符轻微弹入
- **步进器 (+/-)**: 点击后数字放大再缩小；长按连续加减
- **下拉选项**: 展开时选项从上方依次掉落 (stagger)
- **Checkbox/Radio**: 选中时 ✓ 从中心旋转弹入
- **分页器**: 切换页面时数字卡片翻转效果；当前页高亮跳动
- **Tab 切换**: 指示条滑动 + 旧内容向上滑出，新内容向上滑入
- **Toast 通知**: 从右上角弹入 + 轻微弹跳；自动消失时向右滑出
- **空状态插画**: 根据场景不同展示不同的像素插画 (空班级→空教室插画，空礼物→空货架插画)
- **加载骨架屏**: 卡片骨架带流光扫过动画，模拟内容加载

**天气/时间感知装饰：**
```
根据浏览器时间，页面装饰自动变化：
  早上 (6-12点):  ☀️ 阳光 + 彩虹条纹背景
  下午 (12-18点): 🌤️ 白云 + 柔和暖光
  晚上 (18-6点):  🌙 星星 + 深蓝渐变背景，宠物戴睡帽
```

**节日限定装饰：** (根据日期自动切换)
| 日期范围 | 节日 | 装饰 |
|----------|------|------|
| 1月1日前后 | 元旦 | 🎊 彩带 + 金色粒子 |
| 春节期间 | 春节 | 🧧 红包 + 🏮 灯笼 + 红色主题 |
| 6月1日 | 儿童节 | 🎈 气球 + 🎪 马戏团主题 + 宠物戴小丑帽 |
| 9月10日 | 教师节 | 💐 鲜花 + 感谢贺卡弹窗 |
| 10月31日 | 万圣节 | 🎃 南瓜 + 👻 幽灵 + 橙色主题 |
| 12月25日 | 圣诞节 | 🎄 圣诞树 + ❄️ 雪花飘落 + 红绿主题 |

**积分里程碑庆祝：**
```
学生积分达到特定里程碑时，自动触发特殊效果:
  100分:  🌟 银色星星闪烁
  500分:  🥉 铜色奖牌动画
  1000分: 🥈 银色奖杯 + 宠物撒小花
  2000分: 🥇 金色奖杯 + 全屏撒花
  5000分: 👑 皇冠特效 + 全屏大庆祝 + 专属音效
```

### 5.6 趣味细节汇总

- **彩虹分割线**: 用渐变色的 `<hr>` 替代传统分割线，悬停时渐变流动
- **Emoji 雨**: 特殊节日时页面间歇飘落对应主题 emoji
- **像素脚印**: 鼠标点击处留下小脚印/星星，1.5s 后消失
- **宠物小窝**: 右下角宠物旁边放一个小窝/垫子装饰
- **彩蛋交互**: 连续点击宠物 5 次 → 宠物暴走绕屏幕跑一圈；在仪表盘输入「上上下下左右左右」→ 所有卡片跳舞
- **趣味错误页**: 404 页面展示迷路的像素宠物 + "哎呀，走丢了~ 🗺️"

---

## 六、分页与搜索组件设计

### 6.1 通用搜索框组件 `SearchInput`

```
┌────────────────────────────────────────────┐
│  🔍 [___________________________ ]  🎲 随机 │
│      placeholder: "搜搜看..."              │
│                                            │
│  特性:                                     │
│  - 输入时实时搜索 (300ms debounce)          │
│  - 右侧 🎲 按钮: 随机跳转到一个学生         │
│  - 聚焦时边框彩虹渐变流动                   │
│  - 输入字符逐个弹入动画                     │
│  - 支持 keyboard shortcut: Ctrl+K / ⌘K     │
└────────────────────────────────────────────┘
```

### 6.2 通用分页器组件 `Pagination`

```
                    ┌─────────────────────────────┐
                    │ 共 128 条  🦊                 │
                    │                              │
  « 首页  ‹ 上一页   [1] [2] [3] [4] [5] ... [13]   › 下一页  » 末页
                    │      ▲                       │
                    │  当前页(弹跳高亮)              │
                    └─────────────────────────────┘

特性:
  - 页码切换时有翻转动画 (rotateY)
  - 当前页带弹跳高亮效果
  - 超过 7 页自动折叠中间页码
  - 首页/末页按钮在首尾页时自动禁用并半透明
  - 小屏自动切换为紧凑模式: ‹ 3/13 ›
  - 右上角小动物图标随翻页切换表情
```

### 6.3 各页面搜索+分页布局

**班级管理页 (`/classes`)：**
```
┌──────────────────────────────────────────────────────┐
│  📚 班级管理                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │
│  │🔍 搜索班级... │  │ 排序: [最新▼] │  │ [+ 添加班级]  │  │
│  └─────────────┘  └─────────────┘  │ [📥 批量导入]  │  │
│                                    └──────────────┘  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ 班级卡片│ │ 班级卡片│ │ 班级卡片│ │ 班级卡片│       │
│  └────────┘ └────────┘ └────────┘ └────────┘       │
│  ... (每页 12 个)                                    │
│                    « ‹ 1 2 3 › »                     │
└──────────────────────────────────────────────────────┘
```

**学生列表页 (`/classes/[id]`)：**
```
┌──────────────────────────────────────────────────────┐
│  ← 返回     📚 三年级一班     [📥 导入] [+ 添加学生]  │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │🔍 搜索学生... │  │ 排序: [积分▼] │  │🏆 积分排行榜│  │
│  └─────────────┘  └──────────────┘  └────────────┘  │
│                                                      │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ 学生卡片│ │ 学生卡片│ │ 学生卡片│ │ 学生卡片│       │
│  └────────┘ └────────┘ └────────┘ └────────┘       │
│  ... (每页 12 个)                                    │
│                    « ‹ 1 2 3 › »                     │
└──────────────────────────────────────────────────────┘
```

**兑换台 (`/exchange`)：**
```
┌──────────────────────────────────────────────────────┐
│  🎪 积分兑换小铺                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │🔍 搜索礼物... │  │🏷 价格: [全部▼]│  │👤 学生: [选择▼]│  │
│  └─────────────┘  └─────────────┘  └─────────────┘  │
│  当前余额: ⭐ 250 分                                  │
│                                                      │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ 礼物卡片│ │ 礼物卡片│ │ 礼物卡片│ │ 礼物卡片│       │
│  └────────┘ └────────┘ └────────┘ └────────┘       │
│  ... (每页 12 个)                                    │
│                    « ‹ 1 2 3 › »                     │
└──────────────────────────────────────────────────────┘
```

**记录总览 (`/records`)：**
```
┌──────────────────────────────────────────────────────┐
│  📊 记录总览                            [Tab: 全部 | 积分 | 兑换] │
│  ┌─────────────┐  ┌─────────────┐  ┌──────┬──────┐  │
│  │🔍 搜索...    │  │👤 学生: [全部▼]│  │ 📅 日期范围  │  │
│  └─────────────┘  └─────────────┘  └──────┴──────┘  │
│                                                      │
│  ┌────────────────────────────────────────────────┐  │
│  │ 📋 记录时间线                                   │  │
│  │  🟢 小明 +5分  积极回答问题    5分钟前          │  │
│  │  🔴 小红 -3分  未完成作业      10分钟前         │  │
│  │  🎁 小刚 兑换 棒棒糖 -50分    15分钟前          │  │
│  │  ...                                           │  │
│  └────────────────────────────────────────────────┘  │
│                    « ‹ 1 2 3 ... 25 › »              │
└──────────────────────────────────────────────────────┘
```

### 6.4 分页组件实现要点

```typescript
// 分页器核心逻辑
function getPageRange(current: number, total: number): (number | 'ellipsis')[] {
  if (total <= 7) return Array.from({ length: total }, (_, i) => i + 1);
  
  const pages: (number | 'ellipsis')[] = [1];
  
  if (current > 3) pages.push('ellipsis');
  
  const start = Math.max(2, current - 1);
  const end = Math.min(total - 1, current + 1);
  for (let i = start; i <= end; i++) pages.push(i);
  
  if (current < total - 2) pages.push('ellipsis');
  
  pages.push(total);
  return pages;
}
```

### 6.5 搜索实现策略

```
搜索输入 (客户端 state)
    │
    ▼
useDebounce(search, 300ms)
    │
    ▼
更新 URL searchParams (?search=xxx&page=1)
    │
    ▼
触发 Server Component 重新渲染 (RSC)
    │
    ▼
Server 端 SQL 查询:
  SELECT * FROM students
  WHERE class_id = $1
    AND name ILIKE '%' || $2 || '%'
  ORDER BY created_at DESC
  LIMIT $3 OFFSET $4
    │
    ▼
返回分页数据 + 渲染
```

**搜索优化：**
- 客户端 300ms debounce 避免频繁请求
- 搜索时自动重置到第 1 页
- URL 参数同步，支持浏览器前进/后退
- 空结果展示对应场景插画：「没有找到"xxx"哦~ 🐾」

---

## 七、主题切换系统

### 7.1 六套主题定义

```typescript
// lib/themes.ts — 完整主题定义

export const themes: Record<ThemeName, ThemeDefinition> = {
  warm: {
    name: 'warm',
    label: '暖橙主题',
    emoji: '🧡',
    description: '默认主题，温暖活泼',
    colors: {
      primary: '#FF8C42', secondary: '#FFD166', accent: '#EF476F',
      background: '#FFF9F0', cardBg: '#FFF5E8', 
      text: '#4A3728', textSecondary: '#8B7E74',
      success: '#06D6A0', danger: '#EF476F', border: '#FFE0C0',
    },
  },
  candy: {
    name: 'candy',
    label: '糖果主题',
    emoji: '🍬',
    description: '粉嫩甜蜜，适合女生多的班级',
    colors: {
      primary: '#FF6B9D', secondary: '#C084FC', accent: '#F472B6',
      background: '#FFF0F5', cardBg: '#FFF5FA',
      text: '#5B2D40', textSecondary: '#9B6B7E',
      success: '#34D399', danger: '#F87171', border: '#FFD6E8',
    },
  },
  forest: {
    name: 'forest',
    label: '森林主题',
    emoji: '🌲',
    description: '清新自然，护眼舒适',
    colors: {
      primary: '#4ADE80', secondary: '#FBBF24', accent: '#2DD4BF',
      background: '#F0FDF4', cardBg: '#F5FFF8',
      text: '#1A3A24', textSecondary: '#5C7A64',
      success: '#22C55E', danger: '#EF4444', border: '#D1FAE5',
    },
  },
  ocean: {
    name: 'ocean',
    label: '海洋主题',
    emoji: '🌊',
    description: '清爽蓝色，沉稳专注',
    colors: {
      primary: '#0EA5E9', secondary: '#06B6D4', accent: '#8B5CF6',
      background: '#F0F9FF', cardBg: '#F5FBFF',
      text: '#1E3A5F', textSecondary: '#6B8AAE',
      success: '#10B981', danger: '#F43F5E', border: '#DBEAFE',
    },
  },
  sunset: {
    name: 'sunset',
    label: '日落主题',
    emoji: '🌅',
    description: '温暖紫色，梦幻氛围',
    colors: {
      primary: '#A855F7', secondary: '#F59E0B', accent: '#EC4899',
      background: '#FDF5FF', cardBg: '#FDF8FF',
      text: '#3B1A4E', textSecondary: '#8B6B9E',
      success: '#06D6A0', danger: '#EF476F', border: '#F3E8FF',
    },
  },
  dark: {
    name: 'dark',
    label: '暗夜主题',
    emoji: '🌙',
    description: '护眼深色，晚上使用舒适',
    colors: {
      primary: '#A78BFA', secondary: '#FBBF24', accent: '#FB7185',
      background: '#1A1A2E', cardBg: '#252540',
      text: '#E8E6F0', textSecondary: '#A09CB8',
      success: '#34D399', danger: '#F87171', border: '#38385A',
    },
  },
};
```

### 7.2 主题切换实现

```
主题存储: teacher_settings.theme 字段 (数据库) + CSS 变量注入

切换方式:
  1. 设置页面选择 → 保存到数据库 → 刷新 CSS 变量 → 即时生效
  2. Header 快捷切换按钮 → 循环切换 (warm→candy→forest→ocean→sunset→dark→warm)
  3. 节日自动覆盖: 春节(红色)、儿童节(糖果)、圣诞节(红绿) 等自动切换

实现架构:
  ┌─────────────────────────────────────────────┐
  │  ThemeProvider (Client Component)           │
  │  ┌─────────────────────────────────────┐   │
  │  │ 读取 teacher_settings.theme          │   │
  │  │ → 合并节日自动检测 (holidays.ts)     │   │
  │  │ → 设置 CSS 自定义属性到 :root        │   │
  │  │ → 所有 Tailwind 组件响应 CSS 变量     │   │
  │  └─────────────────────────────────────┘   │
  └─────────────────────────────────────────────┘

CSS 变量映射:
  :root {
    --color-primary: <theme.colors.primary>;
    --color-secondary: <theme.colors.secondary>;
    --color-accent: <theme.colors.accent>;
    --color-bg: <theme.colors.background>;
    --color-card-bg: <theme.colors.cardBg>;
    --color-text: <theme.colors.text>;
    --color-text-secondary: <theme.colors.textSecondary>;
    --color-success: <theme.colors.success>;
    --color-danger: <theme.colors.danger>;
    --color-border: <theme.colors.border>;
  }

Tailwind 配置扩展:
  colors: {
    primary: 'var(--color-primary)',
    secondary: 'var(--color-secondary)',
    // ... 等
  }
```

---

## 八、音效资源指南

### 8.1 推荐音效来源

以下资源均为 **CC0 (公有领域)** 或 **Royalty-Free** 许可，可免费使用：

| 音效 | 推荐来源 | 下载方式 |
|------|---------|---------|
| **pop.mp3** (点击) | Freesound — Breviceps/UIClick1 | `freesound.org/people/Breviceps/sounds/448086/` |
| **pop.mp3** 备选 | Freesound — DuffyBro/Pop | `freesound.org/people/DuffyBro/sounds/319107/` |
| **coin.mp3** (积分) | Freesound 搜索 "8-bit coin collect CC0" | `freesound.org/search/?q=8-bit+coin+collect&f=license:"Creative+Commons+0"` |
| **coin.mp3** 备选 | LittleRobotSoundFactory "Collect Point" | Freesound 搜索 "8Bit Collect Point" |
| **success.mp3** (成功) | Freesound 搜索 "8-bit success fanfare CC0" | `freesound.org/search/?q=8-bit+success+fanfare&f=license:"Creative+Commons+0"` |
| **error.mp3** (失败) | Zapsplat — 8-Bit Blips Pack | `zapsplat.com/sound-effect-packs/8-bit-blips/` (免费注册下载) |
| **swoosh.mp3** (弹窗) | Freesound — qubodup/Whoosh | `freesound.org/people/qubodup/sounds/60013/` |
| **celebration.mp3** (庆祝) | Zapsplat — 8-Bit Blips Pack | 同上，选择 "win" / "level up" 音效 |

### 8.2 音效集成代码

```typescript
// hooks/useSound.ts
'use client';
import useSoundLib from 'use-sound';

const soundMap = {
  pop: '/sounds/pop.mp3',
  success: '/sounds/success.mp3',
  error: '/sounds/error.mp3',
  swoosh: '/sounds/swoosh.mp3',
  coin: '/sounds/coin.mp3',
  celebration: '/sounds/celebration.mp3',
} as const;

type SoundName = keyof typeof soundMap;

export function useSound(name: SoundName) {
  const [play, { stop }] = useSoundLib(soundMap[name], {
    volume: 0.5,         // 默认音量 (可通过设置调整)
    sprite: undefined,   // 如需雪碧图可配置
    soundEnabled: true,  // 可通过设置关闭
  });
  return { play, stop };
}

// 使用示例
// const { play } = useSound('coin');
// <button onClick={() => play()}>加分</button>
```

### 8.3 音效下载步骤

1. **Freesound.org** (CC0 音效)：
   - 访问 `freesound.org` 注册免费账号
   - 搜索上述关键词，筛选 License: "Creative Commons 0"
   - 下载 MP3 格式，放入 `public/sounds/` 目录

2. **Zapsplat** (8-Bit Blips 包)：
   - 访问 `zapsplat.com` 注册免费账号
   - 下载 "8-Bit Blips" 音效包
   - 解压，挑选合适的音效重命名为对应文件名

3. **自制 8-bit 音效** (备选)：
   - 使用在线工具 [ChipTone](https://sfbgames.itch.io/chiptone) (免费)
   - 可自定义生成 8-bit 风格的 pop/coin/success 音效
   - 导出 WAV → 转为 MP3

---

## 九、核心交互流程

### 9.1 积分兑换完整流程

```
教师登录 → 进入兑换台 → 选择学生(顶部下拉)
  → 浏览礼物卡片 → 点击"兑换"
  → 弹出确认框 (显示学生/礼物/消耗积分/剩余)
  → 确认兑换
  → 后端校验: 积分 ≥ 礼物价格 && 库存 > 0
  → 扣减积分 + 扣减库存 + 写兑换记录
  → 前端: 🎉撒花 + Toast提示 + 刷新积分余额
```

### 9.2 积分加减完整流程

```
班级列表 → 进入班级 → 点击学生卡片的 [+] 或 [-]
  → 弹出积分调整框
  → 选择或输入原因
  → 确认
  → 后端: 写 point_records + 更新 students.points
  → 前端: 积分数字弹跳动画 + Toast
```

### 9.3 批量导入流程

```
班级/学生列表 → 点击"导入"按钮
  → 弹出导入弹窗
  → 选择方式: [上传Excel] [粘贴文本]
  → 上传Excel: 使用 SheetJS 解析，预览数据
  → 粘贴文本: 每行一个名字，逗号分隔
  → 点击"确认导入"
  → 后端: 批量 INSERT
  → 前端: Toast "成功导入 N 条"
```

---

## 十、API 设计

### 通用查询参数规范

所有列表类 GET 接口统一支持以下查询参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `page` | number | 1 | 页码 (从 1 开始) |
| `pageSize` | number | 12 | 每页条数 (最大 100) |
| `search` | string | — | 模糊搜索关键词 (匹配名称) |
| `sortBy` | string | `createdAt` | 排序字段 |
| `sortOrder` | string | `desc` | 排序方向: `asc` / `desc` |

通用分页响应格式：

```typescript
interface PaginatedResponse<T> {
  success: boolean;
  data: T[];
  pagination: {
    page: number;
    pageSize: number;
    total: number;
    totalPages: number;
    hasNext: boolean;
    hasPrev: boolean;
  };
}
```

### 认证

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 手机号+密码登录，返回 JWT Cookie |
| POST | `/api/auth/logout` | 登出，清除 Cookie |

### 班级

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/classes?page=&pageSize=&search=&sortBy=&sortOrder=` | **分页+搜索** 获取班级列表 |
| POST | `/api/classes` | 创建班级 |
| PUT | `/api/classes/[id]` | 更新班级 |
| DELETE | `/api/classes/[id]` | 删除班级 |
| POST | `/api/classes/import` | 批量导入班级 |
| GET | `/api/classes/all` | 获取全部班级 (下拉选择用，不分页) |

### 学生

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/classes/[id]/students?page=&pageSize=&search=&sortBy=points&sortOrder=` | **分页+搜索** 获取班级学生列表 |
| POST | `/api/students` | 添加学生 |
| PUT | `/api/students/[id]` | 更新学生 |
| DELETE | `/api/students/[id]` | 删除学生 |
| POST | `/api/students/import` | 批量导入学生 |
| POST | `/api/students/[id]/points` | 调整积分 (加减) |
| GET | `/api/students/search?q=&classId=` | 跨班级搜索学生 (兑换页选择用) |

### 礼物

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/gifts?page=&pageSize=&search=&sortBy=pointsPrice&sortOrder=` | **分页+搜索** 获取礼物列表 |
| POST | `/api/gifts` | 创建礼物 |
| PUT | `/api/gifts/[id]` | 更新礼物 |
| DELETE | `/api/gifts/[id]` | 删除礼物 |
| PATCH | `/api/gifts/[id]/toggle` | 上架/下架切换 |

### 兑换

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/exchange` | 执行兑换 (student_id + gift_id) |
| GET | `/api/exchange/records?page=&pageSize=&search=&studentId=` | **分页+搜索** 兑换记录 |

### 记录

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/records/points?page=&pageSize=&search=&studentId=&type=&dateFrom=&dateTo=` | **分页+搜索+筛选** 积分变动记录 |
| GET | `/api/records/exchange?page=&pageSize=&search=&studentId=&dateFrom=&dateTo=` | **分页+搜索+筛选** 兑换记录 |

### 仪表盘

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/dashboard/stats` | 获取仪表盘统计数据 |
| GET | `/api/dashboard/recent` | 获取最近动态 (最新 10 条) |

### **[新增]** 管理员 — 学校管理

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/admin/schools?page=&pageSize=&search=` | 分页+搜索 学校列表 |
| POST | `/api/admin/schools` | 创建学校 (自动绑定管理员) |
| PUT | `/api/admin/schools/[id]` | 更新学校信息 |
| DELETE | `/api/admin/schools/[id]` | 删除学校 (级联删除教师/班级/学生) |

### **[新增]** 管理员 — 教师管理

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/admin/teachers?page=&pageSize=&search=&schoolId=` | 分页+搜索 教师列表 |
| POST | `/api/admin/teachers` | 添加教师 (设置手机号+初始密码) |
| PUT | `/api/admin/teachers/[id]` | 更新教师信息 (姓名/角色) |
| PATCH | `/api/admin/teachers/[id]/reset-password` | 重置教师密码 |
| PATCH | `/api/admin/teachers/[id]/toggle-status` | 启用/禁用教师账号 |

### **[新增]** 设置

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 获取当前用户设置 (主题/音效/宠物) |
| PUT | `/api/settings` | 更新用户设置 |
| GET | `/api/settings/themes` | 获取可用主题列表 |

### 上传

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/upload` | 上传礼物图片到 Vercel Blob |

---

## 十一、TypeScript 类型定义

```typescript
// types/index.ts

export interface School {
  id: string;
  name: string;
  adminId?: string;
  adminName?: string;
  teacherCount?: number;
  studentCount?: number;
  createdAt: Date;
}

export type UserRole = 'admin' | 'teacher';

export interface Teacher {
  id: string;
  phone: string;
  name: string;
  schoolId: string;
  schoolName?: string;
  role: UserRole;
  avatarEmoji: string;
  isActive: boolean;
  createdAt: Date;
}

export interface Class {
  id: string;
  name: string;
  teacherId: string;
  studentCount?: number;
  createdAt: Date;
}

export interface Student {
  id: string;
  name: string;
  classId: string;
  className?: string;
  points: number;
  avatarEmoji: string;
  createdAt: Date;
}

export interface Gift {
  id: string;
  name: string;
  description: string;
  imageUrl: string;
  pointsPrice: number;
  stock: number; // -1 = unlimited
  emoji: string;
  isActive: boolean;
  createdAt: Date;
}

export interface PointRecord {
  id: string;
  studentId: string;
  studentName?: string;
  teacherName?: string;
  pointsChange: number;
  reason: string;
  type: 'add' | 'deduct';
  createdAt: Date;
}

export interface ExchangeRecord {
  id: string;
  studentId: string;
  studentName?: string;
  giftId: string;
  giftName?: string;
  giftEmoji?: string;
  pointsSpent: number;
  exchangedAt: Date;
}

export interface ReasonPreset {
  id: string;
  schoolId: string;
  teacherId?: string | null;
  label: string;
  type: 'add' | 'deduct';
}

// ---- 主题与设置 ----

export type ThemeName = 'warm' | 'candy' | 'forest' | 'ocean' | 'sunset' | 'dark';

export interface ThemeDefinition {
  name: ThemeName;
  label: string;
  emoji: string;
  description: string;
  colors: {
    primary: string;
    secondary: string;
    accent: string;
    background: string;
    cardBg: string;
    text: string;
    textSecondary: string;
    success: string;
    danger: string;
    border: string;
  };
}

export type PetType = 'cat' | 'dog' | 'rabbit' | 'hamster';

export interface TeacherSettings {
  id: string;
  teacherId: string;
  theme: ThemeName;
  soundEnabled: boolean;
  soundVolume: number;
  petType: PetType;
  petName: string;
  animationsReduced: boolean;
  updatedAt: Date;
}

// ---- 分页与查询 ----

export interface Pagination {
  page: number;
  pageSize: number;
  total: number;
  totalPages: number;
  hasNext: boolean;
  hasPrev: boolean;
}

export interface PaginatedResponse<T> {
  success: boolean;
  data: T[];
  pagination: Pagination;
}

export interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
}

export interface SearchParams {
  page?: number;
  pageSize?: number;
  search?: string;
  sortBy?: string;
  sortOrder?: 'asc' | 'desc';
}

export interface PointRecordFilters extends SearchParams {
  studentId?: string;
  type?: 'add' | 'deduct';
  dateFrom?: string;
  dateTo?: string;
}

export interface ExchangeRecordFilters extends SearchParams {
  studentId?: string;
  dateFrom?: string;
  dateTo?: string;
}

// 仪表盘统计
export interface DashboardStats {
  classCount: number;
  studentCount: number;
  giftCount: number;
  todayExchangeCount: number;
  totalPointsAwarded: number;
}

export interface RecentActivity {
  id: string;
  type: 'exchange' | 'point_add' | 'point_deduct';
  studentName: string;
  studentEmoji: string;
  description: string;
  points: number;
  createdAt: Date;
}
```

---

## 十二、实施路线图

### 第一阶段: 基础搭建 (预估 2-3 天)

1. 初始化 Next.js 项目 + Tailwind CSS 配置 + CSS 变量主题系统
2. 配置 Vercel Postgres 连接 + 执行建表 SQL + 索引
3. 实现认证系统 (JWT + middleware + role 校验 + login page 密码可见切换)
4. 搭建基础布局 (Sidebar + Header + 主题切换按钮)
5. 基础 UI 组件库 (Button/Card/Modal/Input/Select/Badge/Skeleton/EmptyState/SearchInput/Pagination)
6. 部署到 Vercel + 配置环境变量
7. 手动在数据库中插入首个管理员账号

### 第二阶段: 管理员功能 (预估 1-2 天)

8. 学校管理 CRUD + 列表 + 分页搜索
9. 教师账号管理 (添加/编辑/禁用/重置密码) + 分页搜索
10. 管理员仪表盘 (学校+教师概览)
11. 管理员 Middleware 权限校验

### 第三阶段: 核心功能 (预估 3-4 天)

12. 班级管理 CRUD + 批量导入 (Excel/文本) + 分页搜索
13. 学生管理 CRUD + 批量导入 + 分页搜索
14. 积分加减功能 (预设原因 + 自定义 + 步进器动画)
15. 礼物管理 CRUD + 图片上传 (Vercel Blob) + 分页搜索
16. 积分兑换功能 (校验余额 + 扣减库存 + 事务)
17. 仪表盘统计数据 API + 最近动态

### 第四阶段: 主题与设置 (预估 1-2 天)

18. 6 套主题完整实现 (warm/candy/forest/ocean/sunset/dark)
19. 主题切换组件 (设置页 + Header 快捷切换)
20. 节日自动主题覆盖
21. 设置页面 (主题/音效/宠物/动效)
22. 设置 API + 数据库读写

### 第五阶段: 趣味 UI (预估 3-4 天)

23. 像素宠物完整系统 (多状态机 + 对话气泡 + 多角色可选)
24. 背景漂浮气泡/星星组件
25. 鼠标彩虹拖尾效果
26. 撒花组件 + 积分里程碑特效
27. 所有卡片入场交错动画 + 悬停浮起 + 点击波纹
28. 积分数字弹跳动画 (加分/扣分不同动效)
29. 按钮弹性/脉冲/摇晃/流光全套动画
30. Modal/Toast 弹入弹出动画
31. 页面过渡动画 (AnimatePresence)
32. 下载音效文件 + 集成 8-bit 音效系统
33. 空状态趣味插画 + 骨架屏加载
34. 节日主题装饰 (春节/儿童节/教师节/万圣节/圣诞)
35. 天气/时间感知装饰
36. 彩蛋交互 (连续点击宠物 / 科乐美密码)
37. 积分排行榜 + 排名变化火箭动画

### 第六阶段: 数据展示与优化 (预估 1-2 天)

38. 记录页面 (Tab切换 + 筛选 + 时间线 + 分页搜索)
39. 学生详情页 (积分时间线)
40. 路由保护完善 (admin vs teacher)
41. 错误处理 + Toast 通知完善
42. 响应式适配 (平板可用)
43. 搜索 URL 参数同步 (支持浏览器前进后退)
44. 性能优化 (RSC 缓存 + 图片优化 + 字体子集化)

---

## 十三、关键注意事项

1. **密码可见切换**: 登录页密码默认使用 `type="text"` 明文可见，旁边提供 👁️‍🗨️ 切换按钮可隐藏。符合设计文档"显示密码"的要求，同时保留隐私选择。

2. **不允许自行注册**: 教师账号由**管理员**在后台创建（设置手机号+初始密码），前端无注册入口。登录页底部提示「没有账号？请联系管理员添加」。

3. **角色权限体系**:
   - **管理员 (admin)**: 可创建学校、管理教师账号、管理全校所有数据
   - **教师 (teacher)**: 只能管理自己创建的班级和学生
   - Middleware 需校验 API 路由的角色权限，防止教师访问管理员接口

4. **学校数据隔离**: gifts / reason_presets 归属到 school_id，同一学校的教师共享礼物库和原因预设，不同学校数据完全隔离。

5. **积分不能为负**: 学生积分添加 CHECK 约束 `points >= 0`，后端兑换时需校验余额充足，使用数据库事务保证一致性。

6. **批量导入**: 支持 Excel (.xlsx/.xls) 和纯文本 (每行一个名字) 两种方式。导入前需预览数据，导入后返回成功/失败统计。

7. **图片处理**: 使用 Vercel Blob 存储礼物图片，上传前进行客户端压缩 (限制 2MB)，统一裁剪为 4:3 比例。

8. **安全性**: 所有 API 路由需验证 JWT + 角色，Server Actions 需校验当前登录用户身份和 school_id 归属，防止越权操作。

9. **主题系统**: CSS 变量驱动，6 套主题一键切换。节日自动覆盖（春节红/儿童节糖果/圣诞红绿等）。主题偏好存储在 `teacher_settings` 表。

10. **音效文件**: 需从 Freesound.org (CC0) 和 Zapsplat (Royalty-Free) 手动下载 6 个音效文件（见"音效资源指南"章节），放入 `public/sounds/` 目录。注意遵守各平台的免费使用条款。

11. **教师账号状态**: 管理员可禁用/启用教师账号。被禁用的教师无法登录，但数据保留。删除学校时级联删除所有下属教师、班级、学生数据，需二次确认。
